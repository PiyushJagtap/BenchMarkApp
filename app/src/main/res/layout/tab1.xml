<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent" android:layout_height="match_parent">

    <android.support.v7.widget.CardView xmlns:card_view="http://schemas.android.com/apk/res-auto"
        android:id="@+id/cardview"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@+id/startBenchmark"
        android:layout_margin="5dp"
        android:layout_alignParentStart="true"
        android:layout_marginBottom="40dp"
        android:layout_marginStart="5dp"
        android:layout_marginEnd="5dp"
        android:layout_marginTop="5dp"
        android:visibility="visible"
        card_view:cardCornerRadius="2dp"
        card_view:cardElevation="6dp">

        <TextView
            android:id="@+id/cardtextview"
            android:layout_width="match_parent"
            android:layout_height="259dp"
            android:layout_marginStart="5dp"
            android:layout_marginTop="5dp"
            android:fontFamily="@font/cabin"
            android:text="BenchMark measures the performance of CPU by performing a set  of algorithms and recording the time required to perform test and thus giving result.\n\nClick the below start button to perform benchmark tests and check your devices score."
            android:textColor="@android:color/black"
            android:textSize="20dp"
            android:textStyle="bold" />
    </android.support.v7.widget.CardView>

    <Button
        android:id="@+id/startBenchmark"
        style="?attr/borderlessButtonStyle"
        android:layout_width="149dp"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentStart="true"
        android:layout_marginBottom="150dp"
        android:layout_marginStart="13dp"
        android:background="?attr/colorBackgroundFloating"
        android:elevation="5dp"
        android:text="Start"
        android:textColor="@android:color/holo_red_dark"
        android:textStyle="bold"
        tools:text="Start" />


</RelativeLayout>